beforeEach(function () {
    // Clear the cats array by setting its length to 0
    cats.length = 0;
  
    // Populate the cats array with three cat names: "Milo", "Otis", and "Garfield"
    cats.push("Milo", "Otis", "Garfield");
  });